class PhoneProcessor:
    @staticmethod
    def format_number(value, format_style, strict=True):
        """Formats a phone number according to specified style"""
        # Phone formatting logic here
        
    @classmethod
    def process_column(cls, df, column, format_style, strict=True):
        """Processes phone numbers in a column"""
        # Column processing logic here 